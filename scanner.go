package main

// scanner.go — Filesystem scanning for target and reference photos
// Walks directories recursively, filters by extension, and reads EXIF data
// to classify photos as targets (no GPS) or references (with GPS).
//
// TODO: Implement in Phase 1 (targets) and Phase 2 (references)
