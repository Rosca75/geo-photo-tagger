# Audit Report — CHANGE-REQUEST-2026-04

**Auditor:** Claude Opus 4.7
**Baseline commit:** `e433d61` (CR 2026-04-16 — commit before phase 1)
**Audited commit:** `dd96fdd` (merge of `claude/implement-change-request-2026-04-6qco3` into `main`)
**Date:** 2026-04-17

## Summary

All seven phases of CHANGE-REQUEST-2026-04 were implemented and behave as
prescribed. One dead-code finding (a stale CSS rule) was auto-fixed. Seven
findings are flagged for owner review — all are architectural/file-layout
deviations where the implementer split files to keep each under the 150-line
ceiling mandated by `CLAUDE.md` rule #3. No backup-safety logic (phase 3a/3b/3c,
`WriteGPS` / `UndoGPS` / `ClearBackups` triplet) was modified as part of this
audit, per the "do NOT auto-fix" scope.

## Auto-fixed findings

| Phase | File | Finding | Fix commit |
|---|---|---|---|
| 4 | `static/css/components.css` | `.threshold-btn.active` CSS rule and its referring comment were left behind after the `.threshold-btn` buttons were removed from the HTML. | `a978d69` |

## Flagged for owner review

### Finding 1: `RunSameSourceMatching` placed in a new file instead of `app_match.go`

- **Phase:** 7
- **File:** `app_match_same.go` (new, 86 lines)
- **What the instruction said:** "`RunSameSourceMatching` method on `App` in `app_match.go`".
- **What was implemented:** The method lives in a new sibling file `app_match_same.go` with the comment "Split into its own file to keep app_match.go under 150 lines."
- **Why flagged:** Architectural deviation (different file than prescribed). Out of auto-fix scope.
- **Recommendation:** Keep as-is. The split preserves the 150-line ceiling from `CLAUDE.md` rule #3. Inlining would push `app_match.go` to roughly 220 lines.

### Finding 2: Candidate radio-selection split out of `matcher_ui.js`

- **Phase:** 2 / 4 / 6
- **File:** `static/js/matcher_select.js` (new, 54 lines)
- **What the instruction said:** Multiple phases (2 and 6) implied that `acceptBestCandidate` and `handleCandidateSelect` live inside `matcher_ui.js`.
- **What was implemented:** Both helpers moved to a new `matcher_select.js`. `matcher_ui.js` imports them.
- **Why flagged:** Architectural deviation. Not in auto-fix scope.
- **Recommendation:** Keep as-is. `matcher_ui.js` is currently 149 lines; inlining would break the 150-line ceiling.

### Finding 3: Delta-slider wiring split out of `matcher_ui.js`

- **Phase:** 4
- **File:** `static/js/matcher_slider.js` (new, 32 lines)
- **What the instruction said:** "`matcher_ui.js` has `formatDeltaMinutes(m)` helper" — i.e. the helper lives in `matcher_ui.js`.
- **What was implemented:** `formatDeltaMinutes` and `initDeltaSlider` live in `matcher_slider.js`; `matcher_ui.js` calls `initDeltaSlider()` from `initMatcher`.
- **Why flagged:** Architectural deviation. Behavior matches the spec (live-update label on `input`, commit to state on `change`).
- **Recommendation:** Keep as-is. Same 150-line rationale as Finding 2.

### Finding 4: Per-row rendering split out of `table.js`

- **Phase:** 6
- **File:** `static/js/table_row.js` (new, 88 lines)
- **What the instruction said:** Phase 6 implied row-building and selection helpers live in `table.js`.
- **What was implemented:** `buildRow`, `toggleRowSelection`, and `selectPhoto` moved to `table_row.js`. `table.js` is now 111 lines and only owns header/sort/select-all.
- **Why flagged:** Architectural deviation.
- **Recommendation:** Keep as-is.

### Finding 5: DNG backup split across two files

- **Phase:** 3c
- **Files:** `dng_backup.go` (165 lines) and `dng_backup_undo.go` (74 lines)
- **What the instruction said:** `dng_backup.go` may also have `dng_backup_undo.go` "if the split was taken" — so this split was anticipated.
- **What was implemented:** Split taken; `dng_backup.go` holds capture/load/sweep and is **165 lines** (over the 150-line ceiling).
- **Why flagged:** Backup-safety code is "do NOT auto-fix" per the audit rules (phase 3 scope). `dng_backup.go` exceeds the line ceiling; further splitting would touch safety-critical code.
- **Recommendation:** Accept the 165-line overage as a conscious exception for backup safety, or schedule a dedicated refactor change-request to split `captureDNGBackup` and `SweepOrphanedSidecars` out.

### Finding 6: `dng_gps_verify.go` exceeds the 150-line ceiling

- **Phase:** 3b
- **File:** `dng_gps_verify.go` (172 lines)
- **What the instruction said:** Phase 3b said to add `verifyGPSInDNG`, `readGPSCoordsFromIFD`, `readThreeRationals` — it did not prescribe a line ceiling for the new file specifically, though `CLAUDE.md` rule #3 applies.
- **What was implemented:** All three helpers present and functionally correct. File is 172 lines.
- **Why flagged:** Backup-safety scope — "do NOT auto-fix". Any split would touch direct-binary TIFF I/O code.
- **Recommendation:** Same as Finding 5 — accept or schedule a focused split CR.

### Finding 7: `writeGPSToJPEG` split out of `exif_writer.go`

- **Phase:** 3b (side-effect)
- **File:** `exif_writer_jpeg.go` (new, 84 lines)
- **What the instruction said:** Phase 3b prescribed modifications inside `exif_writer.go` (routing in `writeAndVerify`); it did not prescribe a JPEG-specific split.
- **What was implemented:** `writeGPSToJPEG` and `decimalToDMS` moved to `exif_writer_jpeg.go`. `exif_writer.go` is now 145 lines.
- **Why flagged:** `exif_writer.go` is part of the backup-safety triplet (`WriteGPS` / `UndoGPS` / `ClearBackups`). Any restructure of this file is in the "do NOT auto-fix" bucket.
- **Recommendation:** Keep as-is. The split keeps the safety-critical file under the ceiling and isolates the JPEG encoding pipeline.

### Finding 8: Two frontend files still exceed the 150-line ceiling

- **Phase:** 3c / 5
- **Files:**
  - `static/js/actions.js` — 184 lines (grew via phase 3c `buildApplyWarning` + multi-line `showConfirm`)
  - `static/js/detail_render.js` — 156 lines (grew via phase 5 map toggle wrapper + `buildGPSPreview` map slot)
- **What the instruction said:** Not called out explicitly, but `CLAUDE.md` rule #3 applies.
- **What was implemented:** Both files grew past the ceiling in the course of phases 3c and 5. No split was taken.
- **Why flagged:** Splitting these would go beyond the auto-fix scope ("style drift" is OK; refactor across multiple functions is not).
- **Recommendation:** Schedule a follow-up to extract `showConfirm` into its own module (the obvious split in `actions.js`) and move `buildCandidatesSection` + `buildGPSPreview` into a sibling file.

## Build + test results

- `wails build -platform windows/amd64`: Not run (Linux CI environment; Windows toolchain unavailable). `go build ./...` succeeds with no output.
- `go test ./...`: ✅ `ok github.com/Rosca75/geo-photo-tagger  0.015s [no tests to run]`
- `go vet ./...`: ✅ clean (no output)

## Benchmark numbers

No `samples/IMGP8411.DNG` present in the audit environment (samples directory
contains only `IMG_2656.HEIC` and `IMG_2656.jpg`), so every benchmark skipped
as designed:

```
BenchmarkApplyGPS_FullPipeline      --- SKIP
BenchmarkApplyGPS_BackupOnly        --- SKIP
BenchmarkApplyGPS_PatchOnly         --- SKIP
BenchmarkApplyGPS_VerifyFullReader  --- SKIP
BenchmarkApplyGPS_VerifyFastPath    --- SKIP
PASS
```

The `b.Skipf` guards prescribed in phase 3a are therefore verified as
functional — benchmarks do not fail in environments without the sample DNG.

## Closing

Implementation quality is high. Every behavioral checkbox in §A.3 of
`CHANGE-REQUEST-2026-04-FOLLOWUP.md` is green. Structural deviations are all in
the same direction: splitting files to stay under the 150-line ceiling, with
explanatory comments at the top of each new file ("Split out of X to keep Y
under the 150-line limit"). That is exactly the discipline `CLAUDE.md` rule #3
asks for — the audit findings above are bookkeeping, not corrections.

Two forward-looking observations for future change requests:

1. When a phase's instructions mandate adding code to a file that is already
   near the ceiling (e.g. `exif_writer.go` at the start of phase 3b), the
   change request should explicitly prescribe where the split goes, so the
   implementer doesn't have to decide between "follow the letter of the
   instruction" and "obey rule #3".
2. `actions.js` and `detail_render.js` should be on the short-list for the
   next housekeeping pass.
